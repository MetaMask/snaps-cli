// const { snapEval } = require('../../../dist/src/cmds/eval/evalWorker');

// describe('eval', () => {
//   describe('snapEval', () => {

//     afterEach(() => {
//       jest.restoreAllMocks();
//     });

//     it('snapEval successfully executes and logs to console', async () => {
//       await expect(manifest(noDistArgv)).rejects.toThrow('Invalid params: must provide \'dist\'');
//     });
//   });
// });
